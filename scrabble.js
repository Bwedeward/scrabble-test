//tile distribution of how mnay letter there are
//need to know points of each letter

//beginning play time

//score the word
//value of each letter
//count of each letter

// (1 point)-A, E, I, O, U, L, N, S, T, R
// (2 points)-D, G
// (3 points)-B, C, M, P
// (4 points)-F, H, V, W, Y
// (5 points)-K
// (8 points)- J, X
// (10 points)-Q, Z

const alphabet = {a:1, b:3, c:3, d:2, e:1, f:4, g:2, h:4, i:1, j:8, k:5, l:1, m:3, n:1, o:1, p:3, q:10, r:1, s:1, t:1, u:1, v:4, w:4, x:8, y:4, z:10}

function wordScore (word) {
    //which letters are in the word that was played
    //return the point value of each letter
    //add the point values 

    const wordSplit = word.split("")

    // wordSplit.forEach(element => {
    //     if (element = alphabet[element])
    // });

    wordSplit.map()
    

}